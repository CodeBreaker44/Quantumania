// Import our custom CSS
import '../scss/styles.scss'

// Import all of Bootstrap's JS
import * as bootstrap from 'bootstrap'

// search entire web page for a string

function search() {
    input = document.getElementById("search").value;
    console.log(input);
}

